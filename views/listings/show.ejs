<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WonderLust</title>
</head>
<body>
    <h3><%= myListing.title %></h3>
    <ul>
            <li><%= myListing.description %></li>
            <li><%= myListing.Listingimage %></li>
            <li> &#x20B9; <%= myListing.price.toLocaleString("en-IN") %></li>
            <li><%= myListing.location %></li>
            <li><%= myListing.country %></li>
    </ul>
    <a href="/listings/<%= myListing._id %>/edit">Edit this listing</a>
    <form method="post" action="/listings/<%= myListing._id %>?_method=DELETE">
        <button>Delete</button>
    </form>

</body>
</html>